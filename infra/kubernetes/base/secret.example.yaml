apiVersion: v1
kind: Secret
metadata:
  name: finpulse-secrets
  namespace: finpulse
type: Opaque
stringData:
  KAFKA_BOOTSTRAP_SERVERS: REPLACE_WITH_MSK_BROKERS
  WAREHOUSE_DSN: REPLACE_WITH_REDSHIFT_DSN
# Production deployments should sync these values from AWS Secrets Manager using External Secrets.

